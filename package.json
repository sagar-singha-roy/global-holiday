{
  "name": "global-holidays-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Global Holidays Monorepo: User site, Admin CRM, and Express Backend",
  "scripts": {
    "dev:user": "cd user && npm run dev",
    "dev:admin": "cd admin && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build:user": "cd user && npm run build",
    "build:admin": "cd admin && npm run build",
    "seed:backend": "cd backend && npm run seed"
  }
}
